# AI应用开发 — 面试问答实战手册

> 使用说明：这是面试模拟对话——每个问题都是面试官可能问的，每个答案都是你应该说的。打印出来，找朋友模拟面试，或者自己对着镜子练。

---

## 第一章：开场自我介绍

### Q1：请简单介绍一下你自己

**你应该说（2分钟版）**：

> "我独立开发了三个AI项目，都在GitHub开源。
>
> 第一个是8个AI应用组成的生态平台，覆盖智能客服、内容生成、素材管理、多Agent协作、模型微调这些场景。技术栈用了Vue3做前端、FastAPI写后端、LangChain+DeepSeek驱动AI、Docker Compose编排17个容器统一部署。最核心的成果是：从零搭建了完整的RAG检索增强生成管道，通过SQLite到PostgreSQL的性能优化，把50并发的失败率从62%降到了6%。还建立了完整的CI/CD流水线和压力测试体系。
>
> 第二个是参考Claude Code架构自建的AI Coding Agent，实现了Skill分层路由来解决Token成本问题、自进化记忆沉淀让经验跨会话复用、分层上下文压缩保证长会话稳定性。
>
> 第三个是多模态RAG知识库系统，支持PDF和图片，基于RAGAS做检索质量量化评测。
>
> 我的优势是能从零独立交付完整的AI应用——前端到后端到AI到部署全链路都会。代码质量方面，8个项目的ruff检查零问题，pytest单元测试全部通过。"

**如果面试官打断你说"挑一个项目重点讲"**：
直接跳到下面Q3或Q4，选一个最对口的展开。

---

### Q2：你为什么要做这些项目？

**你应该说**：

> "三个原因。第一，我想验证自己能不能从零独立交付一个完整的AI产品——不是调个API就完了，而是真正把前端、后端、AI管道、数据库、Docker部署、CI/CD全链路打通。第二，我对RAG和Agent这两个方向特别感兴趣，想通过实战深入理解它们的原理和工程挑战。第三，我发现在学校里学的很多理论，到实际工程中会遇到各种意想不到的问题——比如LangChain的花括号冲突、SQLite的并发锁、Nginx的SSE缓冲——这些是在课本上学不到的。"

---

## 第二章：核心项目深入问答

### Q3：给我讲讲你的AI应用生态平台，最核心的技术是什么？

**你应该说**：

> "最核心的是RAG检索增强生成管道，我分三个阶段来设计：
>
> **离线索引阶段**——把知识库文档变成可检索的向量。用户上传PDF或Word文档后，先用LangChain的加载器解析内容，然后做一个中文感知的分块处理，确保每个分块的语义是完整的。接着用BGE-M3模型把每个分块转成1024维的向量，存到ChromaDB里。
>
> **在线检索阶段**——用户提问时找到最相关的文档。我先对问题做分类路由——比较型问题、事实型问题、关系型问题用不同的检索策略。然后用向量语义搜索加上BM25关键词搜索做混合检索，两种结果通过RRF融合去重，取Top 5最相关的。
>
> **在线生成阶段**——把检索到的文档作为上下文，加上系统提示词和用户问题，构建一个完整的Prompt喂给DeepSeek。DeepSeek通过SSE流式逐token返回，前端实时显示，每个回答下面还标注了引用的来源——告诉用户这个答案依据的是哪个文档的哪一段。
>
> 整个管道我还做了三层降级设计——ChromaDB挂了用内存检索、DeepSeek挂了用Mock模式——保证系统在任何情况下都不会崩溃。"

**面试官可能追问**：用户的问题和知识库里的文档表述不一样怎么办？

**你应该说**：
> "这就是为什么向量检索比关键词检索好。打个比方，用户问'这玩意儿咋退'，如果用关键词搜索，'这玩意儿'在文档里根本找不到。但向量检索用的是语义相似度——'这玩意儿咋退'和'退货政策'在语义空间里方向非常接近，向量相似度能达到0.9以上。所以我用向量检索做主、BM25关键词检索做补充——BM25专门处理'iPhone 15 Pro Max 256GB'这种精确型号的查询。"

---

### Q4：你这个AI Coding Agent是怎么设计的？凭什么说你理解了Agent架构？

**你应该说**：

> "做这个项目的起因是——我天天用Claude Code写代码，觉得它太强了，就想搞清楚它背后是怎么设计的。研究之后我总结出四个核心机制，然后自己实现了一遍：
>
> **第一，Skill路由系统**。问题是这样的：Agent能用的工具越来越多，如果把100个Skill的描述全塞进LLM的上下文，每次调用的Token成本就会爆炸，而且LLM面对100个选择也容易选错。我设计了一个二阶段路由——第一阶段用规则做粗筛，从100个候选里筛出最相关的20个；第二阶段用LLM做精排，从20个里选最匹配的1到3个。这样Token消耗降低了60%以上，选择准确率也提高了。
>
> **第二，自进化记忆沉淀**。每次Agent执行任务都会积累经验——这次bug是怎么修的、用户偏好什么代码风格、哪些Skill组合效果好。如果没有记忆系统，这些经验在下次会话就全丢了。我设计了一个闭环：执行任务→反思提取经验→分类存储到记忆库→下次遇到类似任务自动检索注入上下文。三种记忆——程序性的教Agent'怎么做'、情景性的记录'做过什么'、语义性的存'知道什么'。
>
> **第三，分层上下文压缩**。长会话上下文会越来越长，最后超出LLM的窗口限制。我做了四层压缩：把大工具输出压缩成一句话摘要、把大文件替换成占位符需要时再检索、把讨论过程提炼成结构化笔记、最后如果还是超限就按重要度删减。同时刻意把系统提示词放在prompt最前面固定位置，最大化LLM服务商的Prompt Cache命中率。
>
> **第四，多Agent协作和安全审查**。我设计了中心化架构——一个主Agent统一规划和审批，子Agent以Tool Call方式受控执行。这和Cursor/Windsurf的架构思路是一致的——工业界实践证明了中心化比去中心化更适合代码场景。"

**面试官可能追问**：你说参考Claude Code，它和你实现的有哪些区别？

**你应该说**：
> "Claude Code是商业产品，背后的模型能力、工具链深度、上下文窗口都远超我的实现。比如Claude Code有200K上下文窗口，一个文件再大也能全塞进去；我只能靠压缩来模拟。但架构思想是相通的——Query Loop加Tool Use的执行闭环、Skill系统、记忆机制、分层上下文管理——我通过自己实现一遍，真正理解了这些机制为什么这样设计、每个机制解决了什么具体问题。"

---

### Q5：8个项目听起来很多，它们之间有什么关联吗？

**你应该说**：

> "有，我设计了四条跨项目联动链路：
>
> **第一条，数据飞轮**。客服系统产生的对话数据、销售培训系统的评分记录，会自动推送到数据中心进行清洗和标注。数据中心导出微调格式后，模型工厂就用这些数据做微调训练。训练好的模型部署为推理API后，又可以供客服和培训系统使用——这就是一个数据闭环。
>
> **第二条，素材共享**。素材管理平台提供了一个公开搜索接口，不需要登录就能搜。内容创作和视频脚本工坊在创作时可以直接搜素材库，复用已有的图片和视频。
>
> **第三条，Agent编排调度**。多Agent协作平台可以识别用户意图中的关键词——比如用户说'帮我生成一篇产品文案'——它识别到'文案'这个关键词，就自动调用内容创作系统。用户不用知道背后是哪个系统在响应。
>
> **第四条，模型上线复用**。模型工厂部署的微调模型，其他项目可以通过统一的推理API调用。客服系统可以切换使用通用模型或某个业务定制模型。
>
> 这些联动是通过Docker网络DNS实现的——容器之间用服务名就能通信，不写死IP。共享代码放在一个shared目录里，包括数据推送客户端和跨项目动作注册表。"

---

## 第三章：RAG技术深度问答

### Q6：RAG是什么？和直接调ChatGPT有什么区别？

**你应该说**：

> "RAG是Retrieval-Augmented Generation的缩写，就是把检索和生成结合的技术。
>
> 直接用ChatGPT有三个致命问题。第一，知识截止日期——它不知道训练之后发生的事情，比如公司刚更新的退货政策。第二，幻觉——它可能编造一个看起来很合理但实际不存在的答案。第三，不可溯源——你不知道它的答案依据是什么，没法验证。
>
> RAG的解决思路是：用户提问后，先从知识库里检索最相关的文档，再把文档作为上下文喂给LLM生成回答。这样LLM的回答就是基于真实文档的，不是靠记忆或瞎猜。而且我还在每个回答下面标注了来源——'这个回答依据的是《退货政策》文档第3页'——用户可以点进去验证。
>
> 打个比方：直接调ChatGPT相当于问一个'博学但记性不好还不承认的朋友'，他什么都敢回答但可能不对。RAG相当于给这个朋友面前放了一本'准确的最新资料'，要求他只能照着资料回答，还要注明引用出处。"

---

### Q7：用户问一个问题，从后台接收到返回答案，中间发生了什么？

**你应该说**：

> "整个流程大概十步，但面试时我说五个关键阶段：
>
> **第一步，接收和校验**。API收到请求，JWT中间件验证用户身份，通过后进入业务逻辑。
>
> **第二步，查询分类**。先判断这个问题的类型——是问事实参数、是比较产品、还是闲聊打招呼。不同类型用不同检索策略——问'多少钱'这种事实型，向量检索就够了；问'A和B有什么区别'这种比较型，需要更多检索源。
>
> **第三步，混合检索**。向量语义搜索理解'意思相近'的文档——'退货操作'能匹配到'退款流程'。BM25关键词搜索精确匹配产品型号——'iPhone 15 Pro Max 256GB'。两种结果通过RRF融合去重，取Top 5。
>
> **第四步，构建Prompt**。把检索到的5个文档块标记为[来源:1]到[来源:5]，加上系统提示词——定义客服的角色和规则——再加上用户的问题，拼成完整的Prompt。
>
> **第五步，SSE流式输出**。DeepSeek生成答案，每生成一个token就通过SSE推给前端。前端实时显示——用户看到的是打字机效果，一个字一个字出来，不用等全部生成完。最后把完整回答、引用来源和响应延迟都存到数据库。"

---

### Q8：向量检索和关键词检索有什么区别？为什么两个都要用？

**你应该说**：

> "向量检索擅长理解语义——'怎么退货'和'退款流程'虽然用词完全不同，但意思接近，向量相似度高。但向量检索对精确术语不敏感——'SKU-8848'这种精确型号，向量可能匹配到'SKU-8849'。
>
> 关键词检索相反——'SKU-8848'能精确匹配，但'怎么退货'在关键词模式下永远找不到'退款流程'，因为没一个词相同。
>
> 所以我把两个结合——向量负责语义覆盖，关键词负责精确匹配。就像找书：向量检索是'推荐和这本主题相似的书'，关键词检索是'精确查找ISBN=XXX的书'。两者互补。"

---

### Q9：你怎么保证检索到的文档真的是相关的？

**你应该说**：

> "三道关卡。第一，相似度阈值——向量检索返回的结果，相关性分数低于0.5的直接丢弃。这意味着和问题语义完全不相关的内容不会进入LLM的上下文。
>
> 第二，RRF融合去重——向量检索和关键词检索可能返回同一个文档，RRF保留分数更高的那个版本，避免重复浪费LLM的上下文空间。
>
> 第三，查询分类路由——不同问题类型用不同检索策略。比如用户问'这个产品多少钱'，是简单的事实查询，向量检索就够了，不需要关键词检索和图谱检索。但如果用户问'A和B有什么区别'，是复杂的比较查询，需要启动全部检索源。分类路由避免了'杀鸡用牛刀'——把无关的内容塞进上下文反而会干扰LLM。"

---

### Q10：你的RAG管道里，哪一步最难？为什么？

**你应该说**：

> "最难的是分块策略。分块听起来简单——把长文档切成小块——但影响巨大。切太大，每个块信息多但噪声也多，检索精度下降；切太小，信息不完整，LLM看到的只是知识碎片。
>
> 我做了三件事来优化分块。第一是中文感知——分隔符不是简单地用换行，而是按段落、句子、子句的层次来切。第二是重叠——相邻块之间重叠200个字符，防止关键信息恰好落在边界上。比如'7天内无条件退款'和'款后15天可换货'如果切在两个块里，两个块都匹配不到完整的退货政策。第三是元数据绑定——每个块都标记了来源文档、页码和位置，用于最终回答的引用标注。"

---

## 第四章：SSE与流式输出问答

### Q11：你怎么实现ChatGPT那种逐字输出的效果？

**你应该说**：

> "用的是SSE，Server-Sent Events——一种基于HTTP的单向推送技术。
>
> 原理是这样的：后端返回的不是普通JSON，而是一个特殊的流式响应。DeepSeek每生成一个字，后端就通过这个流推给前端一个字。前端用JavaScript逐字接收到后，像打字机一样显示在页面上。
>
> 三个设计细节很重要。第一，我用的是原生fetch而不是axios——因为axios不支持读取流式响应体。第二，我加了AbortController取消机制——用户连续发多条消息时，自动取消前一条还没走完的流，防止旧消息把新消息覆盖了。第三，我设置了2分钟超时——如果DeepSeek生成太久，不会让用户一直等，而是友好提示。
>
> 这个方案还有一个容易被忽略的关键点：如果你用了Nginx做反向代理，必须把proxy_buffering关掉。Nginx默认会把后端响应攒够了一起发给客户端，SSE流就变成了'等了10秒然后突然看到完整答案'。"

---

### Q12：SSE和WebSocket有什么区别？为什么不选WebSocket？

**你应该说**：

> "SSE是单向的——服务器推给客户端。WebSocket是双向的——两边可以来回发消息。
>
> 我选SSE的原因很直接——聊天场景99%是单向的：服务器推内容给用户。SSE更简单，基于标准HTTP协议，不需要额外的握手和心跳。浏览器对SSE还有内置的自动重连机制——连接断了会自动重连。
>
> WebSocket的优势在于双向实时通信——比如在线协作编辑、实时游戏。如果用WebSocket做聊天也可以，但需要额外处理：建立WebSocket连接需要HTTP升级握手、需要心跳保活防止连接被代理掐断、需要自己实现重连逻辑。
>
> 如果以后要做'对方正在输入…'这种实时状态同步，那就需要WebSocket了——因为那是客户端告诉服务器'我在打字'，需要双向通道。"

---

## 第五章：性能优化与数据库问答

### Q13：你做的性能优化中，最让你有成就感的是哪一个？

**你应该说**：

> "最有成就感的是SQLite到PostgreSQL的性能优化——这是一个很完整的排查和解决故事。
>
> 我用Locust做50并发的压力测试，最开始用SQLite的默认配置，失败了62%的请求。错误主要是'database is locked'——SQLite的默认模式下，写操作要加独占锁，所有读操作都要等。
>
> 我分三个阶段解决。第一阶段，启用WAL模式。WAL的核心思想是把写操作写到独立的日志文件里，读操作继续读原始数据——读和写不再互斥。加上5项PRAGMA调优，失败率从62%降到了30%。
>
> 第二阶段，调优连接池。SQLAlchemy默认连接池参数不适合并发场景，我调整了连接池大小和溢出上限，失败率再降到10%。
>
> 第三阶段，切换到PostgreSQL。PostgreSQL用MVCC多版本并发控制，读和写天然不冲突，失败率最终降到了6%。我设计成通过环境变量切换——开发环境用SQLite(零配置方便)，生产环境用PostgreSQL(高性能)。
>
> 这个故事让我真正理解了数据库并发控制的原理——不是背概念，而是亲手经历了三种模式的性能差异。"

---

### Q14：如果用户量从100涨到10万，你的系统哪里会先出问题？怎么解决？

**你应该说**：

> "这是系统设计的经典问题，我按优先级回答。
>
> **最先崩的是数据库连接数**。PostgreSQL默认连接数有限，10万用户峰值时，每个请求都要一个数据库连接，很快就不够用了。解决方案是加pgBouncer连接池——它复用连接，把几千个应用请求映射到几十个数据库连接上。还可以做读写分离——读请求走只读副本，写请求走主库。
>
> **第二个瓶颈是LLM API调用**。如果每个用户提问都实时调DeepSeek，费用和延迟都会暴涨。解决方案是加Redis缓存——完全相同的问题直接返回缓存答案，相似问题用语义缓存。客服场景80%的问题都是重复的常见问题，缓存可以拦截大部分调用。
>
> **第三个瓶颈是文件上传处理**。大量用户同时上传文档做解析和向量化，同步处理会阻塞响应。解决方案是用Celery异步任务队列解耦——上传后立即返回'处理中'，后台Worker异步处理文档。
>
> **第四个是单机限制**。Docker Compose只能在单机上跑。需要迁移到K8s做自动扩缩容——流量高峰期自动增加容器副本，低谷期回收。
>
> 总结来说就是：数据库连接池 → Redis缓存 → 异步队列 → 水平扩展，按这个顺序逐步演进。"

---

## 第六章：认证与安全问答

### Q15：JWT认证是怎么做的？和Session有什么不同？

**你应该说**：

> "JWT是无状态认证——服务器不存登录状态，所有信息都在令牌本身。Session是有状态的——服务器存一份，客户端只拿一个Session ID。
>
> JWT的优势是扩展性好——加一台新服务器不需要同步Session数据。劣势是不能主动撤销——令牌签发后到过期前，服务器没法让它失效。
>
> 我的做法是折中：访问令牌有效期设为60分钟——很短，即使泄露影响也有限。刷新令牌7天——用来换新的访问令牌。我还在JWT里加了一个type字段来区分访问令牌和刷新令牌——防止攻击者拿刷新令牌当访问令牌用。
>
> 关于主动撤销的问题，生产环境我会加一个Redis黑名单——用户改密码或登出时，把旧令牌的ID加入黑名单，验证时先查黑名单。但这会部分牺牲JWT的无状态优势。"

---

### Q16：你的系统有哪些安全措施？

**你应该说**：

> "几个方面。认证方面：JWT双令牌加类型验证、bcrypt密码哈希防止暴力破解、忘记密码接口永远返回相同的成功消息防止用户名枚举——攻击者不能用'该用户不存在'的错误来筛选活跃用户列表。
>
> API安全方面：每个请求都要验证令牌的type字段必须是access；令牌验证后还要去数据库重查用户状态——即使令牌有效但用户被管理员禁用了，请求也会被拒绝。
>
> 文件安全方面：上传文件用SHA256做内容哈希去重防止重复上传；删除文件前用realpath验证路径确实在允许的目录内——防止路径遍历攻击。
>
> 代码安全方面：GitHub Actions CI会自动扫描提交中是否有硬编码的API密钥；Pre-commit钩子会检测私钥文件被误提交。"

---

## 第七章：前端与全栈问答

### Q17：前端状态管理怎么做的？Pinia和Vuex有什么区别？

**你应该说**：

> "我用Pinia做状态管理——Vue3官方推荐的状态管理库。
>
> 相比Vuex，Pinia有三个让我选它的理由。第一是API简洁——不用区分mutation和action，直接一个方法就能改状态，少了很多样板代码。第二是TypeScript支持好——类型自动推断，不需要手动写类型。第三是多Store天然支持——每个Store独立，不用像Vuex那样嵌套module。
>
> 我还用了持久化插件——用户的登录状态和暗色模式偏好存到localStorage，刷新页面也不丢失。为了防止localStorage数据损坏导致整个应用崩溃，我在初始化时加了try-catch——如果读取到损坏数据就清除重来。
>
> 最值得说的是我修复了一个Token刷新时的并发问题：同一个页面上三个API请求同时遇到401过期，如果没有队列机制，三个请求会各自去刷新Token，导致互相覆盖。我设计了一个Promise队列——第一个请求触发刷新时把后续请求放入等待队列，刷新完成后统一重试。这保证了用户无感——不会有三个请求同时失败的体验。"

---

### Q18：你是前端后端都做吗？你觉得全栈的优势是什么？

**你应该说**：

> "是的，8个项目全部是我从零独立搭建的——前端Vue3、后端FastAPI、Docker部署、CI/CD流水线。
>
> 全栈最大的优势是'端到端的效率'。比如我设计一个API时，脑子里已经知道前端会怎么调用、数据格式长什么样——不需要等前端同学来沟通确认。反过来，前端发现接口返回的数据结构不好用时，我可以直接改后端，不需要跨团队协调。
>
> 第二个优势是'排错更快'。比如SSE流式输出出问题——到底是前端解析错了还是后端没推送？全栈的情况下我能从浏览器的Network面板一路追踪到后端的异步生成器，快速定位。
>
> 当然也要承认——全栈意味着每个方向都不会特别深。我的前端能做到'功能完整、类型安全、体验好'，但要做到'React Fiber的原理'或者'WASM优化'这种深度，还需要在专职前端岗位上锻炼。"

---

## 第八章：Docker与部署问答

### Q19：17个Docker容器是怎么编排的？服务之间怎么通信？

**你应该说**：

> "我用一个docker-compose.yml文件编排了17个容器——1个共享PostgreSQL加8个后端加8个前端。
>
> 关键设计有几点。第一，服务发现靠Docker DNS——容器之间用服务名通信，比如前端访问后端写的是http://p1-backend:8000，不是写死的IP。第二，启动顺序靠健康检查——PostgreSQL先启动并健康检查通过后，后端才开始启动；后端启动后，前端才启动。第三，数据持久化靠命名卷——文件上传、向量数据库、PostgreSQL数据都映射到宿主机，容器删了数据不丢。
>
> 前端容器还有一个特别的设计：我把源码目录以只读卷的方式挂载进容器。这样修改代码后容器内Vite自动检测变化并热重载——不需要重新构建镜像，开发效率很高。"

---

### Q20：Docker Compose和Kubernetes有什么区别？

**你应该说**：

> "Docker Compose适合单机多容器场景，Kubernetes适合多机集群场景。
>
> 核心区别有几个。第一，调度——Compose只能在一台机器上跑，K8s可以在集群中选择最优节点自动部署。第二，扩缩容——Compose需要手动指定副本数，K8s可以根据CPU内存自动扩缩。第三，滚动更新——K8s支持零停机更新，Compose会短暂中断。第四，服务发现——Compose靠DNS，K8s有内置的Service和Ingress做服务暴露和负载均衡。
>
> 我的项目目前用Compose就够了——17个容器在一台机器上跑，适合开发和小规模部署。如果以后要生产化，我的docker-compose.yml里的每个service都天然对应一个K8s的Deployment加Service加ConfigMap，迁移路径是清晰的。"

---

## 第九章：AI/Agent深度问答

### Q21：LangChain有什么坑吗？

**你应该说**：

> "踩过三个比较深的坑。
>
> 第一个是花括号冲突。我在Prompt里写了一个示例`{功能}+{场景}={效果}`想给AI参考，结果LangChain的ChatPromptTemplate把所有花括号都当成模板变量去解析，直接报KeyError。解决方法是双写花括号`{{功能}}`来转义。本质原因是LangChain底层用了Python的str.format。
>
> 第二个是spaCy依赖地狱。我想用LangChain的UnstructuredLoader解析文档，结果它底层依赖spaCy——一个超过100MB的NLP模型。在Docker镜像里装spaCy，构建时间翻了好几倍。最后我用TextLoader加自定义分块器绕过了这个依赖。
>
> 第三个是流式输出的工件清理。DeepSeek流式返回时，偶尔会把内部AIMessageChunk的元数据片段泄露到输出里——用户会看到一些奇怪的Python对象字符串。我加了一个正则清理层把这些工件过滤掉。
>
> 总结一句话：**LangChain帮你省80%的时间，但剩下20%的坑要你自己填。**"

---

### Q22：RAG和微调有什么区别？什么时候用哪个？

**你应该说**：

> "RAG是把知识放在外面，微调是把知识写进模型。
>
> RAG适合知识频繁变化的场景——比如产品目录每周更新、政策法规经常修订。你只需要更新知识库文档，不需要重新训练模型。而且RAG的答案可以溯源——每个回答都标注了依据哪个文档。
>
> 微调适合需要学习特定表达方式的场景——比如让模型学会用保险行业的专业术语、学会用某个品牌的语气说话。但微调的知识是冻结在训练时点的——训练之后的新知识模型不知道。
>
> 实际中两者是互补的。我的项目里就是这么做的——先用RAG提供实时准确的文档信息，模型工厂则负责微调让模型更理解特定领域的表达习惯。就像培养一个客服：微调是教他说'您好，很高兴为您服务'这种专业话术；RAG是给他一本实时更新的产品手册。"

---

### Q23：Prompt工程有什么心得？

**你应该说**：

> "我总结了一个'角色-规则-示例-约束-格式'五要素法：
>
> **角色**：给AI一个明确的身份。不是'你是一个助手'，而是'你是一个有3年经验的电商客服，态度热情专业，对产品了如指掌'。角色越具体，AI的语气越到位。
>
> **规则**：告诉AI什么可以做、什么不能做。比如'严格基于知识库回答，不要编造信息'——这句话把RAG的核心约束说清楚了。但规则不要太多，超过7条AI就容易顾此失彼。
>
> **示例**：给一两个Few-shot例子。比如'用户问退货流程，你应该这样回答…'。但示例需要和当前任务相关——给客服看文案的示例就是误导。
>
> **约束**：设置技术参数。客服场景temperature设0.3保证事实准确；创意文案场景设0.8增加多样性。
>
> **格式**：明确输出格式。'用[来源:N]标注引用'、'用markdown表格呈现对比信息'。
>
> 一个具体的例子：我在客服Prompt里写了'严格基于知识库，不知道就说不知道'——这一句话就让RAG的可靠性大幅提升，LLM不会在检索为空时瞎编。"

---

### Q24：你用了哪些Agent相关的技术？Agent和普通LLM调用的本质区别是什么？

**你应该说**：

> "我做了多Agent协作平台和AI Coding Agent两个Agent相关的项目。
>
> Agent和普通LLM调用的本质区别是：普通LLM调用是你问它答，一问一答就结束了。Agent是'自主决策'——它拿到任务后自己拆解步骤、自己选择使用哪个工具、自己判断任务是否完成。
>
> 核心是三个机制。**规划**——把大任务拆成小步骤。**工具调用**——不只是输出文字，还能执行代码、搜索文件、调用外部API。**反馈循环**——工具执行的结果作为输入进入下一轮决策，形成'思考→行动→观察→再思考'的回环。
>
> 我的多Agent项目实现了四种协作模式——流水线让Agent顺序传递结果、并行让多个Agent从不同角度分析、投票让Agent民主决策、辩论让Agent充分对弈——这些模式背后都是Agent在自主规划下一步该怎么做。"

---

### Q25：你提到了Prompt Cache，能解释一下吗？

**你应该说**：

> "Prompt Cache是LLM服务商的一个优化机制。简单说就是——如果两个请求的前面部分完全相同，服务商就不会重新计算那部分，直接复用第一次的结果。
>
> 打个比方：你每次点外卖，地址是不变的——只改菜名。但如果你把地址写在点菜列表的最前面，每次外卖员都要重新看完地址才能看到菜名。Prompt Cache就是让外卖员记住你的地址，只看新增的部分。
>
> 我在Agent的分层上下文压缩里刻意利用了这一点——把系统提示词和Skill描述这些不变的东西放在prompt最前面，把用户输入这些变化的东西放在最后面。这样两次请求的前面部分完全相同，第二次就能命中缓存——速度更快、成本更低。"

---

## 第十章：知识空白诚实回答

### Q26：你项目中有用到Redis吗？

**你应该说（诚实但展示学习意愿）**：

> "目前还没有，但我很清楚Redis在我的项目里应该怎么用。第一，做RAG的热点问题缓存——用户问'退货怎么操作'，这是高频问题，不用每次都调LLM，直接从Redis返回缓存答案。第二，做Token黑名单——用户登出或改密码后，把旧Token加入Redis，实现主动撤销。
>
> 第三，做Celery的消息代理——异步处理文档上传。我在自己做项目时优先解决了核心的AI管道问题，Redis是下一步优化的计划。整个方案我已经想清楚了，只需要落地。"

---

### Q27：你了解Kubernetes吗？

**你应该说（诚实但展示理解）**：

> "我的项目目前用Docker Compose部署，适合单机场景。Kubermetes我了解核心概念——Pod是最小调度单元、Service做服务发现、Deployment管理滚动更新、HPA根据负载自动扩缩容、Ingress做外部流量入口。
>
> 我的docker-compose.yml里17个service，每个都对应一个K8s的Deployment加Service加ConfigMap，迁移路径是清晰的。但深入使用K8s——比如Helm Chart编排、RBAC权限配置、网络策略——这些我还需要在实际项目中锻炼。"

---

### Q28：你有高并发系统的经验吗？

**你应该说（诚实但展示思考深度）**：

> "我的项目是单机架构，没有真正的高并发场景。但我通过压力测试验证了系统的性能边界——用Locust模拟50并发，发现了SQLite的锁竞争问题，通过WAL模式到PostgreSQL的优化把失败率从62%降到6%。
>
> 如果要做真正的高并发，我理解的演进路径是：先加缓存层(Redis)拦截热点请求；再异步化解耦耗时操作(Celery消息队列)；然后做数据库读写分离和连接池优化；最后做水平扩展(K8s自动扩缩容加负载均衡)。每一层解决了什么瓶颈、什么时候该加——这些思路我是清楚的。"

---

## 第十一章：高频追问集

### Q29：你做的项目有用户吗？怎么验证的？

> "有完整的测试体系。单元测试覆盖了8个项目的认证和核心业务逻辑——pytest在GitHub Actions上并行跑。压力测试用Locust模拟真实用户行为——从注册登录到提问到反馈，每个项目有55个真实业务场景的测试问题。压测报告用HTML和CSV输出，P50/P95/P99延迟一目了然。
>
> 当然，测试不等于生产用户。我的项目定位是完整的技术实践——证明我能从零交付一个可用的AI产品。真正的用户验证需要产品化运营，这是我欠缺的。"

---

### Q30：你做项目时遇到过团队协作的挑战吗？

> "这些项目是我独立开发的，所以没有传统意义上的团队协作经验。但独立开发也锻炼了一些可迁移的能力——比如我会给每个提交写清晰的commit message方便别人理解、用Pre-commit和CI保证代码质量在团队中保持一致、代码结构和注释清楚到别人能看懂。
>
> 我也清楚团队开发和独立开发的核心区别——独立开发不需要沟通、不需要代码评审、不需要处理意见分歧。如果进入团队，我会重点关注代码评审流程和团队编码规范。"

---

### Q31：你对AI领域的发展怎么看？下一个突破点在哪里？

> "我认为AI Agent会是下一个重要方向。现在大家都还在用'一问一答'的聊天模式——你问它答，然后结束了。但Agent可以自主拆解任务、调用工具、执行多步操作——比如你让它'调研竞品并写一份报告'，它会自己去搜索、整理、分析、写稿，不需要你一步一步引导。
>
> 上下文窗口的扩大也在改变游戏规则。以前做RAG是因为模型只能处理几千字，必须检索精炼后再喂。现在200K甚至1M的上下文窗口出现后，'要不要直接把所有文档塞进去'变成了一个实际的工程选择——RAG和长上下文的边界在变得模糊。
>
> 还有一个趋势是多模态——文字+图片+视频+语音的大一统。比如我的多模态RAG项目已经在尝试图片检索，但视频理解那部分还没做。接下来视频内容的AI理解和检索是很有价值的方向。"

---

### Q32：如果重新设计你的项目，你会改什么？

> "三个改变。第一，我会一开始就选PostgreSQL而不是SQLite——虽然SQLite方便开发启动，但后期迁移有成本。第二，我会加一层Nginx反向代理——现在17个容器各自暴露端口，Nginx统一入口在端口管理和安全性上都更好。第三，Agent之间的通信我会用标准的MCP协议而不是自己定义的HTTP调用——标准化后更容易扩展和维护。
>
> 但我不后悔从简单方案开始——如果一开始就想做大而全的架构，可能一个项目都完不成。先跑起来再优化，比规划完美但永远没落地强。"

---

## 第十二章：记忆速查与面试策略

### 面试时必须脱口而出的关键数字

```
62→30→6        # SQLite失败率优化：标准→WAL→PostgreSQL
17容器/814文件  # 项目规模
8项目/100+API   # 项目覆盖
4种Agent编排    # Pipeline/Parallel/Vote/Debate
3层降级         # ChromaDB→内存→降级提示
0.3/0.5/Top5   # temperature/相似度阈值/检索数量
60分钟+7天      # access token/refresh token有效期
```

### 面试前一天复习清单

| 时间 | 做什么 |
|------|--------|
| 前晚 | 通读全文，标记要重点准备的问题 |
| 前3小时 | 对着镜子练自我介绍(Q1)、RAG管道(Q3/Q7)、性能优化(Q13) |
| 前1小时 | 快速翻本章高频追问(Q29-Q32)、知识空白(Q26-Q28) |
| 前30分钟 | 看关键数字、深呼吸 |
| 面试中 | 本文档放旁边（远程面试时ALT+TAB快速查） |

### 三种面试节奏

| 公司类型 | 主推项目 | 重点讲什么 | 关键词 |
|----------|---------|-----------|--------|
| AI应用公司 | 8项目生态+RAG | 管道设计、降级、流式、Prompt | RAG、SSE、Agent |
| 全栈/后端 | 8项目生态+Docker | 架构、性能优化、CI/CD、部署 | FastAPI、PostgreSQL、Docker |
| 大厂/研究院 | MiniCode Agent | Agent架构、记忆系统、路由 | Agent、Context Engineering |

### 被问到不会的时候怎么办

> "这个方向我目前了解不深。我接触过的相关概念是[如果知道任何相关内容就说]，但深入实践还需要学习。我的学习方法是[举一个你学新东西快的例子]——比如我这个AI应用生态就是一个月内从零搭建出来的。"

---

## 附录：8项目一句话速查

| # | 项目名 | 一句话介绍 | 面试重点讲什么 |
|---|--------|-----------|---------------|
| ① | RAG智能客服 | 知识库问答+转人工+引用溯源 | RAG全链路、SSE、性能优化 |
| ② | 自媒体内容助手 | 5平台文案/标题/脚本生成 | Prompt工程、多平台适配 |
| ③ | 短视频脚本工坊 | 分镜表+TTS语音+字幕导出 | 结构化JSON输出 |
| ④ | DAM素材管理 | AI自动标签+文搜图+毛玻璃UI | 竞态修复、上传队列、安全 |
| ⑤ | 销售培训系统 | AI扮演客户+5维评分 | LLM Judge评估 |
| ⑥ | 数据中心 | 数据飞轮中枢+清洗标注 | 跨项目联动 |
| ⑦ | 多Agent协作 | 4种编排+SSE实时展示 | Agent架构 |
| ⑧ | 模型微调平台 | 智能推理+LLM Judge+缓存 | Smart Proxy三层架构 |

---

## 第十四章：项目整体框架总览

> 这一章是面试开场的"一张图讲清楚你在做什么"——面试官让你介绍项目时，先扔框架再展开细节。

### 14.1 一句话讲清楚我的项目

> "我做了一个8项目AI应用生态，覆盖客服、内容创作、素材管理、多Agent协作、模型微调6种业务场景。每个项目独立前后端+AI管道+Docker部署，通过跨项目联动形成完整数据飞轮。所有项目GitHub开源，有完整CI/CD。"

### 14.2 整体架构（面试时在白板上画）

```
┌──────────────────────────────────────────────────────┐
│                   前端层 (Vue 3 × 8)                  │
│  NaiveUI + Pinia + VueRouter + Vite + TypeScript      │
│  8个独立前端，端口 3000-3008                            │
├──────────────────────────────────────────────────────┤
│                   API网关层 (FastAPI × 8)              │
│  JWT双令牌认证 + SSE流式 + CORS + 速率限制             │
│  8个独立后端，端口 8000-8808                            │
├──────────┬──────────┬──────────┬─────────────────────┤
│ C端 × 3  │ B端 × 2  │ 中台 × 3 │     共享服务         │
│ ①客服    │ ④素材管理│ ⑥数据中心│  PostgreSQL × 1     │
│ ②自媒体  │ ⑤销售培训│ ⑦多Agent│  (8个独立数据库)     │
│ ③短视频  │          │ ⑧微调   │  ChromaDB(向量)      │
├──────────┴──────────┴──────────┴─────────────────────┤
│                    AI能力层                           │
│  LangChain + DeepSeek + ChromaDB + BGE-M3嵌入         │
│  RAG管道 | SSE流式 | Agent编排 | Prompt工程           │
├──────────────────────────────────────────────────────┤
│                   部署运维层                          │
│  Docker Compose (17容器) + GitHub Actions CI/CD       │
│  Pre-commit Hooks + pytest + Locust压力测试            │
└──────────────────────────────────────────────────────┘
```

### 14.3 三层业务定位

| 层 | 项目 | 面向谁 | 解决什么问题 |
|----|------|--------|-------------|
| **C端应用** | ①客服 ②自媒体 ③短视频 | 普通用户 | 用AI提升日常效率——问答、创作、视频 |
| **B端平台** | ④素材管理 ⑤销售培训 | 企业用户 | 用AI解决企业内部资产管理、员工培训 |
| **中台服务** | ⑥数据中心 ⑦多Agent ⑧微调 | 开发者 | 数据中台+AI调度+模型定制，支撑C/B端 |

### 14.4 跨项目数据流（四条联动链路）

```
链路1 数据飞轮: ①客服对话 + ⑤培训记录 → ⑥数据中枢(清洗标注)
              → ⑧模型工厂(微调训练) → 部署推理API → 回馈①②③⑤

链路2 素材共享: ④素材管理(公开搜索) → ②自媒体 + ③短视频(创作时引用)

链路3 Agent编排: ⑦多Agent平台(关键词识别) → HTTP调用①②③④⑤⑥⑧

链路4 模型上线: ⑧模型工厂(微调部署) → 推理API → ①②③⑤切换定制模型
```

### 14.5 一个用户请求的完整路径

```
浏览器(前端:3001)
    ↓ HTTP请求 + JWT Bearer Token
Nginx反向代理(预留)
    ↓
FastAPI后端(:8101)
    ↓ get_current_user — JWT验证 + 数据库重查用户状态
    ↓ get_db — 获取异步数据库会话
服务层(编排业务逻辑)
    ↓
RAG管道:
  检索知识库(向量+BM25混合检索 → Top5最相关文档)
    ↓
  构建Prompt(系统提示词 + 检索文档[标注来源] + 用户问题)
    ↓
  DeepSeek流式生成 → SSE逐token推送前端
    ↓
保存消息 + 引用来源 + 响应延迟到数据库
    ↓ 异步后台任务
推送数据中心(数据飞轮: 对话记录→项目⑥)
```

### 14.6 技术选型全景对比

| 选了 | 为什么 | 不选什么 |
|------|--------|---------|
| **FastAPI** | 原生异步+自动API文档+Pydantic校验 | Django太重，Flask异步不原生 |
| **Vue3** | 模板直观+Pinia简洁+中文组件好 | React需适应JSX，状态库碎片化 |
| **LangChain** | 文档加载器+LCEL链式+流式原生 | 直接调API代码量3-5倍 |
| **DeepSeek** | 中文同级GPT-4，成本低90%+ | GPT-4贵且需代理 |
| **ChromaDB** | pip install零部署，够用 | Milvus需K8s独立服务 |
| **PostgreSQL** | JSONB列存AI数据+asyncpg快 | MySQL JSON功能弱 |
| **SSE** | 单向+HTTP原生+自动重连 | WebSocket双向增加复杂度 |
| **Pinia** | API简洁+TS友好+多Store天然 | Vuex样板代码多 |

---

## 第十五章：AI应用开发面试题——背诵版

> 这一章是"考前划重点"——面试前1小时快速翻一遍，每道题一句话核心答案+关键词。

### 15.1 LLM & AI 基础

**Q: LLM和传统NLP的本质区别？**
> 传统NLP：单任务专模型，需单独训练。LLM：通用大模型，预训练+提示/微调完成多任务，具备涌现能力。

**Q: 大模型"幻觉"是什么？怎么缓解？**
> 模型生成看似合理但错误的内容。原因：基于概率预测而非真实理解。缓解：RAG(检索真实文档约束回答)、降低temperature、明确告诉模型"不知道就说不知道"。

**Q: Embedding是什么？为什么能用来搜索？**
> 把文字变成向量。两段文字意思越接近，向量方向越一致。语义搜索=把问题转向量→找方向最近的文档向量。

**Q: Temperature参数怎么选？**
> 控制输出随机性。客服等事实场景用0.3(高确定性)，创意场景用0.7-0.9(多样性)。

**Q: Token是什么？**
> LLM处理文本的最小单位+计费单位。英文1词≈1.3token，中文1字≈2token。DeepSeek约¥1/百万token。

**Q: RAG和微调的区别？**
> RAG：知识在外，实时检索(可更新、可溯源)。微调：知识入模，训练写入(个性化强、推理快)。互补：RAG供实时信息+微调提升领域表达。

**Q: RAG完整流程？**
> 文档切块→嵌入向量化→存向量库。用户提问→向量检索→取Top5→拼接Prompt→LLM生成。RAG解决知识滞后、私有数据无法接入、幻觉三大问题。

**Q: Chunk Size怎么定？**
> 经验值1000字符+重叠200字符。事实型用小块(精确)，分析型用大块(需上下文)。重叠200防止关键信息切在边界。

### 15.2 Agent & AI 架构

**Q: Agent和普通LLM调用的本质区别？**
> 普通调用一问一答。Agent自主规划步骤→选择工具→执行→观察→调整→循环直到完成。核心是"自主决策循环"。

**Q: Agent核心四要素？**
> 大模型(决策大脑) + 规划能力(拆解任务) + 记忆(短期对话+长期向量) + 工具调用(执行操作)。

**Q: ReAct模式？**
> Reasoning+Acting循环。先思考"要做什么"→调用工具→观察结果→再思考。交替直到任务完成。

**Q: Function Calling？**
> 模型自动判断是否需要调用外部函数→返回结构化调用请求(函数名+参数)→开发者执行→结果回传→模型生成自然语言回答。

**Q: Context Window超了怎么办？**
> 分层压缩：删除不重要内容，保留优先级 当前任务>最近对话>用户偏好>历史摘要。同时系统提示词固定最前面最大化Prompt Cache。

**Q: Prompt Cache？**
> LLM服务缓存处理过的Prompt前缀。两次请求前面相同→第二次跳过计算。命中法：不变内容放前面，变化内容放最后。

### 15.3 Python后端 & 数据库

**Q: Python的GIL？**
> 同一时刻只有一个线程执行Python字节码。CPU密集型用多进程，IO密集型用asyncio(单线程协程不受GIL影响)。

**Q: async/await原理？**
> async def定义协程，await挂起让出控制权给事件循环。事件循环是单线程调度器。协程切换显式(await)，线程切换OS抢占。

**Q: FastAPI依赖注入？**
> Depends()运行时解析依赖。可测试(mock注入)、可复用(100+端点共用)、声明式(看签名就知道需要什么)。

**Q: 数据库连接池？**
> 预建N个连接保持活跃，请求直接借用完还。避免每次建连的开销。我的配置：pool_size=20，max_overflow=40，pool_pre_ping=True。

**Q: 乐观锁 vs 悲观锁？**
> 悲观锁：读时加锁(安全但慢)。乐观锁：读时不锁，更新时查版本号(冲突少时快)。SQLite WAL本质是乐观并发控制。

### 15.4 前端 & 全栈

**Q: Vue3响应式原理？**
> Proxy拦截对象属性读写，改变量自动通知组件重渲染。比Vue2的defineProperty强在：可拦截新增属性、数组索引。

**Q: 虚拟DOM？**
> 真实DOM的轻量JS表示。改虚拟DOM(快)→Diff找最小变更→一次更新真实DOM(慢)。省去大量低效DOM操作。

**Q: CORS？**
> 浏览器禁止跨域访问。解决：后端加Access-Control-Allow-Origin头，或Vite proxy转发(浏览器看来同源)。

### 15.5 Docker & DevOps

**Q: Docker解决了什么？**
> 应用+依赖打包成镜像，任何环境运行一致。解决"我机器能跑你跑不了"。

**Q: 镜像 vs 容器？**
> 镜像=类(静态模板只读)。容器=实例(运行进程可读写)。一个镜像启动多个容器。

**Q: Docker Compose vs Dockerfile？**
> Dockerfile定义单容器怎么构建。Compose定义多容器怎么协作(依赖/端口/环境/卷)。我的：8个Dockerfile+1个Compose(17容器)。

**Q: CI/CD？**
> CI每次提交自动测试+lint保证代码质量。CD测试通过自动部署。我实现CI：GitHub Actions四Job矩阵(ruff+pytest×8+vue-tsc+安全扫描)。

### 15.6 安全 & 认证

**Q: 哈希 vs 加密？**
> 哈希单向不可逆(密码→哈希值↛密码)。加密双向可逆(明文↔密文)。密码该哈希(bcrypt)，不能加密。

**Q: bcrypt为什么比MD5好？**
> bcrypt故意慢(100ms/次)→暴力破解从百万次/秒降到10次/秒。内置salt防彩虹表。

**Q: JWT缺点？**
> ①无法主动撤销→加Redis黑名单。②Payload只编码不加密→不存敏感信息。③令牌类型混淆→payload里加type字段。

### 15.7 项目关键数字（面试脱口而出）

```
62→30→6     SQLite标准→WAL→PostgreSQL 50并发失败率
17容器       Docker编排规模
814文件      Git跟踪代码文件数
100+端点     RESTful API总数
4种模式      Agent编排(Pipeline/Parallel/Vote/Debate)
3层降级      ChromaDB→内存→降级提示
0.3/0.5/Top5 temperature/相似度阈值/检索返回数
60min+7day   Access Token/Refresh Token有效期
```

---

> **📌 面试前一天冲刺**：第14章(项目框架)背下来面试开头用 → 第1-12章选对口的项目问答看 → 第15章(背诵版)面试前1小时快速翻
| ⑧ | 模型微调平台 | 智能推理+LLM Judge+缓存 | Smart Proxy三层架构 |
